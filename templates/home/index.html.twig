{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}

<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href=" {{ asset('assets/img/ico/favicon.png') }}">
    <link rel="apple-touch-icon" sizes="144x144" href=" {{ asset('assets/img/ico/apple-touch-icon-144x144.png') }}">
    <link rel="apple-touch-icon" sizes="114x114" href="{{ asset('assets/img/ico/apple-touch-icon-114x114.png') }}">
    <link rel="apple-touch-icon" sizes="72x72" href="{{ asset('assets/img/ico/apple-touch-icon-72x72.png') }}">
    <link rel="apple-touch-icon" href="{{ asset('assets/img/ico/apple-touch-icon-57x57.png') }}">

    <title>GESTION DE STOCK</title>

    <!-- Bootstrap Core CSS -->
    <link href=" {{ asset('assets/css/bootstrap.min.css') }} " rel="stylesheet">
    <link href=" {{ asset('assets/css/animate.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/css/plugins.css') }}" rel="stylesheet">

    <!-- Custom CSS -->
    <link href=" {{ asset('assets/css/style.css') }}" rel="stylesheet">

    <!-- Custom Fonts -->
    <link href="{{ asset('assets/font-awesome-4.1.0/css/font-awesome.min.css') }}" rel="stylesheet" type="text/css">
    <link href="{{ asset('assets/css/pe-icons.css') }} " rel="stylesheet">

</head>

<body id="page-top" class="regular-navigation">


    <div class="master-wrapper">

        <div class="preloader">
            <div class="preloader-img">
            <!-- 	<span class="loading-animation animate-flicker"><img src="assets/img/loading.GIF" alt="loading" /></span>-->
            </div>
        </div>

        <!-- Navigation -->
        <nav class="navbar navbar-expand-lg navbar-dark fixed-top wow animate__animated animate__fadeInDown" 
            data-wow-delay="0.2s">
            <div class="container-fluid">
                <!-- Brand and toggle get grouped for better mobile display -->
                <div class="navbar-header page-scroll">
                    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#main-navigation">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a class="navbar-brand smoothie" href="index.php">AB COMPTA<span class="theme-accent-color">V2</span></a>
                </div>

                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse" id="main-navigation">
                    <ul class="nav navbar-nav navbar-right" style="color: #fff;">
                        <li><a href="#about" class="page-scroll" style="color: #fff;">About Us</a></li>  
                        <!--  <li><a href="#about" class="page-scroll" style="color: #fff;">Specials</a></li> -->   
                        <li><a href="#provenderie" class="page-scroll" style="color: #fff;">Provenderie</a></li>      
                        <li><a href="#pharmacie" class="page-scroll" style="color: #fff;">Pharmacie</a></li>   
                        <li><a href="#Client" class="page-scroll" style="color: #fff;">Client</a></li>                
                        <li><a href="#search" style="color: #fff;"><i class="fa fa-search"></i></a></li>
                    </ul>

                </div>
                <!-- /.navbar-collapse -->         

            </div>
            <!-- /.container-fluid -->
        </nav>

        <div id="search-wrapper">
            <button type="button" class="close">×</button>
            <form>
                <input type="search" value="" placeholder="Enter Search Term" class="form-control" name="search">
                <button type="submit" class="btn btn-primary">Search</button>
            </form>
        </div>

        <!-- Header -->
        

        <section id="about" class="top-border-me">
            <div class="section-inner">

                <div class="container">
                    <div class="row">
                        <div class="col-lg-6 text-center mb100">                        
                            <h2 class="section-heading">WE <span class="theme-accent-color">are</span> Welcome</h2>
                            <hr class="thin-hr">
                            <h3 class="section-subheading secondary-font">Nous sommes très heureux de te voir.</h3>
                        </div>
                        <div class="col-lg-2 text-center mb100">
                            <i class="fas fa-arrow-right" style="font-size: 80px; color: #34c759;"></i>
                        </div>
                        <div class="col-lg-4 text-center mb100">
                            <video width="100%" controls autoplay muted>
                                <source src="{{ asset('uploads/video/a.mp4') }}" type="video/mp4">
                                Your browser does not support the video tag.
                            </video>
                        </div>
                        
                        <div class="col-lg-12 mb100" >
                            {% if app.user %}

                                {% if is_granted('ROLE_ADMIN_ADMIN') %}
                                    <div class="card card-center" style="width: 100%;">
                                        <div class="card-body">
                                            <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                            <h5 class="card-title text-center">Direction Générale</h5>
                                            <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                            <div class="d-flex justify-content-between">
                                                <a href="{{ path('app_home_dashboard', {'id': 0}) }}" class="btn btn-primary">
                                                    Provenderie
                                                </a>
                                                <a href="{{ path('app_home_dashboardA', {'id': 0}) }}" class="btn btn-primary">
                                                    Cabinet
                                                </a>
                                            </div>
                                        </div>
                                    </div>
                                    <br>
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% elseif is_granted('ROLE_CLIENTS') %}
                                    {% if app.user.employer is not empty%}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{app.user.employer.agence.nom }}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': app.user.employer.agence.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': app.user.employer.agence.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    {% else %}
                                        <p class="text-danger">Vous êtes un client. Veuillez vous référer à votre zone de travail.</p>
                                    {% endif %}
                                {% elseif is_granted('ROLE_EMPLOYER') %}
                                    Employer
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% endif %}
                                {% if is_granted('ROLE_DIRECTEUR') %}
                                Directeur
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% elseif  is_granted('ROLE_MANAGER') %}
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% elseif  is_granted('ROLE_COMPTABLE') %}
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% elseif  is_granted('ROLE_SECRETAIRE') %}
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% elseif  is_granted('ROLE_CHAUFFEUR') %}
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% elseif  is_granted('ROLE_TEMPORAIRE') %}
                                    {% for agences in agence %}
                                        <div class="card card-center" style="width: 100%;">
                                            <div class="card-body">
                                                <i class="fas fa-home" style="font-size: 100%; width: 100%; height: 100%; display: flex; align-items: center; justify-content: center;"></i>
                                                <h5 class="card-title text-center">{{ agences.nom}}</h5>
                                                <p class="card-text text-center">Bienvenue dans votre agence. Veuillez accéder à vos services.</p>
                                                <div class="d-flex justify-content-between">
                                                    <a href="{{ path('app_home_dashboard', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Provenderie
                                                    </a>
                                                    <a href="{{ path('app_home_dashboardA', {'id': agences.id}) }}" class="btn btn-primary">
                                                        Cabinet
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                        <br>
                                    {% endfor %}
                                {% else %}
                                    <p class="text-danger">Vous êtes qui ?. Veuillez vous référer à votre zone de travail.</p>
                                {% endif %}
                                    
                            {% endif %}
                        </div>
                    </div>
                </div>

                

            </div>
        </section>

       

        <footer class="white-wrapper">
            <div class="container-fluid">
                <div class="row text-center">
                    <div class="col-md-12 wow fadeIn mb30" data-wow-delay="0.2s">
                        <span class="copyright">GESTION  DE STOCK</span>
                    </div>
                    <div class="col-md-12">
                        <ul class="list-inline social-links wow fadeIn" data-wow-delay="0.2s">
                            <li>
                                <a href="#"><i class="fa fa-twitter"></i></a>
                            </li>
                            <li>
                                <a href="#"><i class="fa fa-pinterest"></i></a>
                            </li>
                            <li>
                                <a href="#"><i class="fa fa-dribbble"></i></a>
                            </li>
                            <li>
                                <a href="#"><i class="fa fa-facebook"></i></a>
                            </li>
                            <li>
                                <a href="#"><i class="fa fa-behance"></i></a>
                            </li>
                            <li>
                                <a href="#"><i class="fa fa-linkedin"></i></a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </footer>

        <div id="bottom-frame"></div>

        <a href="#" id="back-to-top"><i class="fa fa-long-arrow-up"></i></a>

    </div>

    
    

</body>

</html>


{% endblock %}
{% block javascripts %}
  {{ parent() }}
  <script src="{{ asset('assets/js/jquery.js') }}"></script>
  <script src="{{ asset('assets/js/bootstrap.min.js') }}"></script>
  <script src=" {{ asset('assets/js/plugins.js') }}"></script>
    <script type="text/javascript" src="https://maps.google.com/maps/api/js?sensor=false"></script>
  <script src="{{ asset('assets/js/init.js') }}"></script>
    <script type="text/javascript">
        $(document).ready(function(){
        'use strict';
            jQuery('#headerwrap').backstretch([
            "{{ asset('assets/img/bg/bg1.jpg') }}",
            "{{ asset('assets/img/bg/bg2.jpg') }}",
            "{{ asset('assets/img/bg/bg3.jpg') }}",
            ], {duration: 8000, fade: 500});
        });
    </script>
{% endblock %}